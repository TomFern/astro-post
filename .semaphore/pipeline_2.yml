version: v1.0
name: Continuous Deployment
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu2004
blocks:
  - name: Deploy
    task:
      jobs:
        - name: Vercel Deploy
          commands:
            - checkout
            - npm install -g vercel
            - vercel deploy --prod
